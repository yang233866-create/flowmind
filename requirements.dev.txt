# FlowMind AI - dependency manifest (LOCAL DEVELOPMENT, full stack)
# Python 3.12 (managed by uv). Install order matters for torch CUDA:
#   uv pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
#   uv pip install -r requirements.dev.txt
#
# NOTE: This is the full local dependency set (SUMO + RL + YOLO). It is NOT used
# for Streamlit Community Cloud deployment — see requirements.txt (slim).

# Vision
ultralytics>=8.3
supervision>=0.25

# Simulation (eclipse-sumo wheel ships SUMO binaries; SUMO_HOME auto-detected via sumolib)
eclipse-sumo>=1.20
traci>=1.20
sumolib>=1.20

# Reinforcement learning
sumo-rl>=1.4.5
stable-baselines3>=2.4
gymnasium>=0.29
tensorboard
tqdm            # SB3 progress bar
rich            # SB3 progress bar
libsumo==1.27.1 # must match eclipse-sumo version; training speedup

# App / visualization
streamlit>=1.40
plotly>=5.24
matplotlib>=3.9
seaborn>=0.13
pandas>=2.2
opencv-python>=4.10
